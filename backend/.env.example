# Backend memuat environment dari ROOT project, bukan dari folder ini.
#
# Salin template dari root:
#   cp ../.env.example ../.env
#
# Lalu edit ../.env — lihat README.md bagian "Konfigurasi Environment".
