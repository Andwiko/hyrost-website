// Main API routes entry
const express = require('express');
const router = express.Router();

// Example: router.use('/auth', require('./auth'));
// TODO: Add forum, chat, membership, admin routes

module.exports = router;
